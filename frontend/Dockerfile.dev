FROM node:20-alpine

WORKDIR /app

EXPOSE 3000

ENV PORT=3000
ENV HOSTNAME=0.0.0.0

# Command is now handled by docker-compose for better flexibility
CMD ["sh", "-c", "npm install --legacy-peer-deps && npm run dev"]
